'use client'

// BottomNav is kept for any standalone pages that may still reference it.
// The main trip navigation lives in TripShell.
export function BottomNav({ tripId }: { tripId: string; trip?: unknown }) {
  void tripId
  return null
}
